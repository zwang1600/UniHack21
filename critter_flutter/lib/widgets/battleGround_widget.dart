import 'package:flutter/material.dart';

class BattleGroundWidget extends StatelessWidget {
  const BattleGroundWidget();

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 400,
      width: 400,
      color: Colors.grey,
    );
  }
}